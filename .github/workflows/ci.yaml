name: ci

on:
  push:
    branches: [master]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        directories: ['jenkins', 'php7.3-cli']
    steps:
      - uses: actions/checkout@v2
      - run: docker build ${{ matrix.directories }}
